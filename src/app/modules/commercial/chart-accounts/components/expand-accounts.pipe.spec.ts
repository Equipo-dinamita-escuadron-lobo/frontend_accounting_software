import { ExpandAccountsPipe } from './expand-accounts.pipe';

describe('ExpandAccountsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandAccountsPipe();
    expect(pipe).toBeTruthy();
  });
});
