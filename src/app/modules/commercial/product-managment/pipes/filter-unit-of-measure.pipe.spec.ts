import { FilterUnitOfMeasurePipe } from './filter-unit-of-measure.pipe';

describe('FilterUnitOfMeasurePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUnitOfMeasurePipe();
    expect(pipe).toBeTruthy();
  });
});
